apiVersion: 1
datasources:
  - name: PersonalInstructor
    type: postgres
    uid: pi-postgres
    url: db:5432
    user: postgres
    jsonData:
      database: postgres
      sslmode: disable
    secureJsonData:
      password: ${POSTGRES_PASSWORD}